# ⚠️ TEMP — delete this folder when backend is wired

Everything here is **mock/demo data only** (UI-first development).
When the real FastAPI backend is connected:

1. Replace the `get*()` calls with real API calls (`src/lib/api/client.ts`)
2. Delete this entire `src/temp/` folder

No component imports should exist outside of pages/services.