"use client";

// ============================================================
// AiPaperGenerator — tab entry point. Renders the full 16-step
// Paper Builder wizard (blueprint §1.0).
// ============================================================

import PaperBuilder from "./paper-builder/PaperBuilder";

export default function AiPaperGenerator() {
    return <PaperBuilder />;
}
